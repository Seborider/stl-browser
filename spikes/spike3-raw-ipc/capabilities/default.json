{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the spike window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
